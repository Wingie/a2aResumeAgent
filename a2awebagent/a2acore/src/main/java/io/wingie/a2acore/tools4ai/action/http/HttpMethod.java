package io.wingie.a2acore.tools4ai.action.http;

public enum HttpMethod {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD, OPTIONS, TRACE, CONNECT
    // Add other HTTP methods as needed
}
